import IObjectKeys from '../interfaces/IObjectKeys';

export default interface IOperators extends IObjectKeys {
  plus: string;
  minus: string;
  multiple: string;
  divide: string;
  equals: string;
}
