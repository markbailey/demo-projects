import IObjectKeys from '../interfaces/IObjectKeys';

interface IOperators extends IObjectKeys {
  plus: string;
  minus: string;
  multiple: string;
  divide: string;
  equals: string;
}

export default IOperators;
